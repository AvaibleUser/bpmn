import { Component } from '@angular/core';
import { RouterModule } from '@angular/router';

@Component({
  selector: 'shop-landing',
  imports: [RouterModule],
  templateUrl: './landing.html',
})
export class Landing {}
